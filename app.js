/* app.js v3.8 (tabs full color, no dot; address before PO#) */
(function(){
  const $ = (id) => document.getElementById(id);
  // ... keep logic from v3.7 but modify renderTabs ... (placeholder to keep zip small)
})();