[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs"]

[build]
  publish = "."   # 👈 use repo root (or set to the actual folder that has index.html)
  command = ""
